# Repository instructions

- After every complete repository change, run `./scripts/install-macos-app.sh` before handoff.
- Treat `$HOME/Applications/Klaus.app` as the installed development app.
- Verify that the installer succeeds and that the installed app points to this checkout.
- Do not report a change as complete while the installed app is stale.
